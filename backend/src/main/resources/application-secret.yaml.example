ai:
  image:
    # OpenAI가 접근할 수 있는 백엔드 공개 주소입니다. 로컬 주소는 사용할 수 없습니다.
    public-base-url:
  openai:
    # 로컬 전용 값입니다. 실제 값이 담긴 application-secret.yaml은 커밋하지 않습니다.
    api-key:
    text-model:
    vision-model:

# 실제 OpenAI 텍스트 연동 검증: ./gradlew openAiSmokeTest
